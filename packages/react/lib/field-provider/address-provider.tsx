import type { AddressFormField } from '@efie-form/core';

interface AddressProviderProps {
  // Define necessary props here
  field: AddressFormField;
}

export default function AddressProvider(_props: AddressProviderProps) {
  // return createElement()
}
