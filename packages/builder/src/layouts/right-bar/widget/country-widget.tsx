import type { CountryWidget } from '@efie-form/core';

interface CountryWidgetProps {
  widget: CountryWidget;
}

export default function CountryWidget({ widget }: CountryWidgetProps) {
  return <></>;
}
